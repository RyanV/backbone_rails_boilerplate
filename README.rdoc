boiler plate application to use as a base for building new apps

To use, clone the application then rename the application throughout the app.  also don't forget to remove the .git file and run `git init`

=====Short term TODO:
-add rename task to rename application
=====Long term TODO:
-move this into a gem executable that doesn't requiring cloning, renaming, etc.
